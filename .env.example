# ⚠️ 所有 VITE_ 開頭的變數都會被打包進前端 bundle、對使用者完全公開，
#    絕對不可放 API secret、私鑰等機密。
# API base URL（留空則以目前頁面同源為 API origin；Bearer token 只會送往此 origin）
VITE_API_BASE_URL=

# Sentry DSN（留空 = 停用前端錯誤追蹤）。
# DSN 設計上就是可公開的寫入端點，放進 VITE_ 變數是安全的；
# 但 Sentry 的 auth token 絕不可放這裡。
# 填了之後，reportError() / ErrorBoundary / window 層級的未捕捉錯誤都會送進 Sentry。
# 要讓錯誤真的通知到人，還要在 Sentry 建 Slack alert rule。
VITE_SENTRY_DSN=
